# Tirocinium desktop — Windows Local / dev プロファイル
# これを apps/desktop/.env.local にコピーして使う (gitignore 済)。

VITE_SERVER_URL=http://localhost:8084
# VITE_WS_URL=ws://localhost:8084   # 省略時は SERVER_URL から導出

# server の TIROCINIUM_DEV_AUTH=1 と対。ログイン画面に「Dev ログイン」ボタンを出す。
VITE_DEV_AUTH=1
